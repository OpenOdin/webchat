/**
 * Export of globals.
 *
 */

import {
    OpenOdin,
} from "openodin";

import {
    PresenceController,
    ChannelListController,
    NotificationsController,
} from "./controllers";

export default {
    OpenOdin,
    PresenceController,
    ChannelListController,
    NotificationsController,
}
